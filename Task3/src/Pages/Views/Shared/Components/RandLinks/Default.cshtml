@model IEnumerable<Pages.Models.Page>



<ul>
    @foreach (var rp in Model)
    {
        <li><a href="#">@Html.ActionLink("" + @rp.UrlName, "Custom", "Pages", new { id = @rp.PageID })</a></li>
    }
</ul>